package com.example.captainsLog.viewmodel

import androidx.lifecycle.ViewModel
import com.example.captainsLog.data.repository.RecordingRepository


class RecordViewModel(private val repo: RecordingRepository) : ViewModel() {
    // TODO: manage recording state, uploading, and transcription
}
